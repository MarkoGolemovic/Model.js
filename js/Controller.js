/**
 * Responds to user inputs and manages the model.
 */
export class Controller {
    constructor(model, view) {
        this.model = model;
        this.view = view;

        this.view.createInput(JSON.stringify(this.model));

    }

    this.view.inputs.

}
